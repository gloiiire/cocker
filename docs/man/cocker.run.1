.\" "Generated by swift-argument-parser"
.Dd August 10, 2026
.Dt COCKER.RUN 1
.Os
.Sh NAME
.Nm "cocker run"
.Nd "Run a command in a new container"
.Sh SYNOPSIS
.Nm
.Op Fl -detach
.Op Fl -interactive
.Op Fl t
.Op Fl -rm
.Op Fl -name Ar name
.Op Fl -publish Ar publish...
.Op Fl -volume Ar volume...
.Op Fl -env Ar env...
.Op Fl -label Ar label...
.Op Fl -network Ar network
.Op Fl -cpus Ar cpus
.Op Fl m Ar m
.Op Fl -shm-size Ar shm-size
.Op Fl -stop-signal Ar stop-signal
.Op Fl -hostname Ar hostname
.Op Fl -workdir Ar workdir
.Op Fl u Ar u
.Op Fl -restart Ar restart
.Op Fl -cap-add Ar cap-add...
.Op Fl -cap-drop Ar cap-drop...
.Op Fl -privileged
.Op Fl -env-file Ar env-file...
.Op Fl -add-host Ar add-host...
.Op Fl -dns Ar dns...
.Op Fl -dns-search Ar dns-search...
.Op Fl -volumes-from Ar volumes-from...
.Op Fl -tmpfs Ar tmpfs...
.Op Fl -read-only
.Op Fl -health-cmd Ar health-cmd
.Op Fl -health-interval Ar health-interval
.Op Fl -health-timeout Ar health-timeout
.Op Fl -health-start-period Ar health-start-period
.Op Fl -health-retries Ar health-retries
.Op Fl -no-healthcheck
.Ar image
.Op Ar command...
.Op Fl -version
.Op Fl -help
.Sh DESCRIPTION
.Bl -tag -width 6n
.It Fl d , -detach
Run container in the background
.It Fl i , -interactive
Keep STDIN open
.It Fl t
Allocate a pseudo-TTY
.It Fl -rm
Automatically remove container when it exits
.It Fl -name Ar name
Assign a name to the container
.It Fl p , -publish Ar publish...
Publish ports (host:container[/tcp|/udp])
.It Fl v , -volume Ar volume...
Bind mount a volume (src:dst[:ro])
.It Fl e , -env Ar env...
Set environment variables (KEY=VALUE)
.It Fl -label Ar label...
Set metadata labels
.It Fl -network Ar network
Connect container to a network
.It Fl -cpus Ar cpus
Number of CPUs
.It Fl m Ar m
Memory limit in MB
.It Fl -shm-size Ar shm-size
Size of /dev/shm in MB (default: kernel  usually 64). Common values: 1024 for postgres, 2048 for chromium.
.It Fl -stop-signal Ar stop-signal
Signal to use on `cocker stop` before SIGKILL (default SIGTERM)
.It Fl -hostname Ar hostname
Container hostname
.It Fl w , -workdir Ar workdir
Working directory inside the container
.It Fl u Ar u
Username or UID
.It Fl -restart Ar restart
Restart policy (no|always|on-failure[:max]|unless-stopped)
.It Fl -cap-add Ar cap-add...
Add Linux capabilities
.It Fl -cap-drop Ar cap-drop...
Drop Linux capabilities
.It Fl -privileged
Give extended privileges to this container
.It Fl -env-file Ar env-file...
Read environment variables from a file
.It Fl -add-host Ar add-host...
Add an entry to /etc/hosts (name:ip)
.It Fl -dns Ar dns...
Extra DNS server, consulted after cocker's own resolver
.It Fl -dns-search Ar dns-search...
DNS search domain (`cocker` stays appended)
.It Fl -volumes-from Ar volumes-from...
Mount volumes from another container
.It Fl -tmpfs Ar tmpfs...
Mount a tmpfs (e.g. /run or /run:size=64m)
.It Fl -read-only
Mount the container root read-only
.It Fl -health-cmd Ar health-cmd
Command to run to check health (CLI override)
.It Fl -health-interval Ar health-interval
Time between healthchecks (e.g. 5s, 1m). Default 30s.
.It Fl -health-timeout Ar health-timeout
Per-check timeout (e.g. 3s, 1m). Default 30s.
.It Fl -health-start-period Ar health-start-period
Grace window before failures count (e.g. 5s, 1m).
.It Fl -health-retries Ar health-retries
Consecutive failures before unhealthy.
.It Fl -no-healthcheck
Disable any healthcheck (overrides image HEALTHCHECK)
.It Ar image
Image to run
.It Ar command...
Command to run inside the container
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.Sh "EXIT STATUS"
.Ex -std