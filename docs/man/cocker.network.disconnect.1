.\" "Generated by swift-argument-parser"
.Dd August 10, 2026
.Dt COCKER.NETWORK.DISCONNECT 1
.Os
.Sh NAME
.Nm "cocker network disconnect"
.Nd "Move a stopped container back to the default bridge"
.Sh SYNOPSIS
.Nm
.Op Fl -force
.Ar network
.Ar container
.Op Fl -version
.Op Fl -help
.Sh DESCRIPTION
A container is always on some network, so leaving one means returning
to `bridge`. Applies at the next start, for the same reason `connect`
does: the switch port is wired when the VM boots. A running container
is refused rather than reported as disconnected.
.Bl -tag -width 6n
.It Fl f , -force
Not honoured: the disconnect is the same either way
.It Ar network
Network name
.It Ar container
Container ID or name
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.Sh "EXIT STATUS"
.Ex -std