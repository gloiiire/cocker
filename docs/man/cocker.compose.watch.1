.\" "Generated by swift-argument-parser"
.Dd August 5, 2026
.Dt COCKER.COMPOSE.WATCH 1
.Os
.Sh NAME
.Nm "cocker compose watch"
.Nd "Rebuild & restart services on file changes (FSEvents-based)"
.Sh SYNOPSIS
.Nm
.Op Fl -file Ar file
.Op Fl -project-name Ar project-name
.Op Fl -debounce-ms Ar debounce-ms
.Op Fl -detach
.Op Fl -stop
.Op Fl -status
.Op Fl -logs
.Op Fl -attach
.Op Fl -version
.Op Fl -help
.Sh DESCRIPTION
.Bl -tag -width 6n
.It Fl f , -file Ar file
Compose file path
.It Fl p , -project-name Ar project-name
Project name
.It Fl -debounce-ms Ar debounce-ms
Coalesce file events for this many ms before rebuilding (default 300)
.It Fl d , -detach
Run the watch loop in the background. Returns the PID and exits.
.It Fl -stop
Stop a previously detached watch loop for this project.
.It Fl -status
Show whether a detached watch is running for this project.
.It Fl -logs
Tail the detached watch's log file (like `tail -f`).
.It Fl a , -attach
Re-attach : stop the detached watch and resume in the foreground (interactive footer + d/q keys). Containers keep running.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.Sh "EXIT STATUS"
.Ex -std