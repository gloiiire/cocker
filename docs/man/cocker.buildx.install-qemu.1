.\" "Generated by swift-argument-parser"
.Dd August 11, 2026
.Dt COCKER.BUILDX.INSTALL-QEMU 1
.Os
.Sh NAME
.Nm "cocker buildx install-qemu"
.Nd "Download a qemu-user emulator for cross-arch builds"
.Sh SYNOPSIS
.Nm
.Op Fl -arch Ar arch
.Op Fl -version Ar version
.Op Fl -qemu-version Ar qemu-version
.Op Fl -all
.Op Fl -force
.Op Fl -version
.Op Fl -help
.Sh DESCRIPTION
.Bl -tag -width 6n
.It Fl -arch Ar arch
Foreign target architecture to install (x86_64, i386, arm, s390x, ppc64le, riscv64, mips64).
.It Fl -version Ar version
tonistiigi/binfmt release tag (defaults to a known-good pin).
.It Fl -qemu-version Ar qemu-version
QEMU version embedded in the release tarball name.
.It Fl -all
Install every emulator in the bundle, not just --arch.
.It Fl -force
Overwrite existing binaries.
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.Sh "EXIT STATUS"
.Ex -std