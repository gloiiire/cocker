.\" "Generated by swift-argument-parser"
.Dd August 11, 2026
.Dt COCKER.AUTOCOMPLETE.INSTALL 1
.Os
.Sh NAME
.Nm "cocker autocomplete install"
.Nd "Copy the Kiro/Fig autocomplete spec into ~/.fig/autocomplete/build/"
.Sh SYNOPSIS
.Nm
.Op Fl -source Ar source
.Op Fl -dry-run
.Op Fl -version
.Op Fl -help
.Sh DESCRIPTION
Looks for the spec at every standard Homebrew prefix (and falls
back to a path relative to the running `cocker` binary so source
installs work too), then copies it to
~/.fig/autocomplete/build/cocker.ts. Restart your terminal
afterwards to pick up the change.

Idempotent  re-running just overwrites the previous copy.
.Bl -tag -width 6n
.It Fl -source Ar source
Explicit path to cocker.ts (skips auto-discovery)
.It Fl -dry-run
Print what would happen without modifying anything
.It Fl -version
Show the version.
.It Fl h , -help
Show help information.
.El
.Sh "EXIT STATUS"
.Ex -std